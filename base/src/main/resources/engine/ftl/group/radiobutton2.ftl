<#list 1..4 as index>
    <label style="margin-left :3px;"></label> 
	<input type="radio" tipPosition="bottom" name="${panelObj.name}_${g.name}" 
	id="${panelObj.name}_${g.name}_${index}" />
</#list>
